<view class="accountDesc" style="background-image: url({{account.coverUrl}})" bindtap="viewDetail" data-account="{{account}}">
  <view class="accountName">
    <view>{{account.inputValue}}</view>
    <view class="accountTime">{{account.now}}</view>
  </view>
  <slot></slot>
</view>